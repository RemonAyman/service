# مشروع الخدمات (Services Project) 🚀

يا أهلاً بيك يا بطل في التيم! ده دليل بسيط عشان تعرف تشغل المشروع عندك على الجهاز وتطلع قماش في أسرع وقت.

المشروع ده مبني بـ **Laravel 10** في الباك-إند (Back-end) و **React + Vite** في الفرونت-إند (Front-end).

---

## 🛠 المتطلبات الأساسية (Prerequisites)
قبل ما تبدأ، اتأكد إن الحاجات دي متسطبة عندك:
1. **PHP >= 8.1** (يفضل 8.2).
2. **Composer** (عشان مكتبات لارفيل).
3. **Node.js & npm** (عشان جافا سكريبت والمكتبات بتاعتها).
4. **MySQL** أو أي سيرفر داتابيز (زي XAMPP أو Laragon).

---

## 🚀 خطوات التشغيل (Setup Steps)

امشي ورا الخطوات دي بالترتيب وعيش:

### 1. تنزيل المكتبات (Dependencies)
افتح التيرمينال (Terminal) جوه فولدر المشروع واكتب:
```bash
# نزل مكتبات الـ PHP
composer install

# نزل مكتبات الـ JavaScript
npm install
```

### 2. إعداد ملف الـ Environment
هتلاقي ملف اسمه `.env.example` خده كوبي وسميه `.env`:
```bash
cp .env.example .env
```
بعد كده افتح الـ `.env` وظبط بيانات الداتابيز بتاعتك:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=service  # اسم الداتابيز اللى هتعملها
DB_USERNAME=root     # اليوزر نيم بتاعك
DB_PASSWORD=         # الباسورد بتاعك
```

### 3. توليد مفتاح المشروع (Generate App Key)
عشان لارفيل يشتغل صح:
```bash
php artisan key:generate
```

### 4. تجهيز الداتابيز (Database & Migration)
اعمل داتابيز جديدة على السيرفر بتاعك بنفس الاسم اللي كتبته في الـ `.env` (غالباً `service`) وبعدين رن الأمر ده:
```bash
# هيرفع الجداول ويبدأ يحط بيانات تجريبية (لو موجودة)
php artisan migrate --seed
```

---

## 💻 إزاى ترن المشروع (Running the App)

عشان تشوف الشغل بتاعك، هتحتاج تفتح **2 تيرمينال**:

- **الأولى (عشان الباك-إند):**
  ```bash
  php artisan serve
  ```
  ده هيشغل المشروع على: `http://127.0.0.1:8000`

- **الثانية (عشان الفرونت-إند والصور):**
  ```bash
  npm run dev
  ```
  ده هيشغل **Vite** عشان أي تغيير تعمله في الـ React يظهر فوراً في المتصفح.

---

## 📁 هيكل المشروع السريع
- **Routes:** هتلاقيها في `routes/api.php` و `routes/web.php`.
- **Frontend (React):** موجود بالكامل في `resources/js`.
- **Controllers:** موجودة في `app/Http/Controllers`.

---

لو واجهتك أي مشكلة، اسأل أي حد من الشباب في التيم أو ابعتلنا فوراً.. بالتوفيق يا وحش! 💪✨⚡
